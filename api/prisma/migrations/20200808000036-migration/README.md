# Migration `20200808000036-migration`

This migration has been generated by Chris Ellis at 8/7/2020, 8:00:36 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "new_StoreCategory" (
"id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
"order" INTEGER NOT NULL,
"categoryName" TEXT NOT NULL,
"categoryId" INTEGER NOT NULL,
"storeId" INTEGER NOT NULL,
FOREIGN KEY ("storeId") REFERENCES "Store"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

INSERT INTO "new_StoreCategory" ("id", "order", "categoryId", "storeId") SELECT "id", "order", "categoryId", "storeId" FROM "StoreCategory"

PRAGMA foreign_keys=off;
DROP TABLE "StoreCategory";;
PRAGMA foreign_keys=on

ALTER TABLE "new_StoreCategory" RENAME TO "StoreCategory";

PRAGMA foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200806232101-migration..20200808000036-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource DS {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider      = "prisma-client-js"
@@ -31,14 +31,14 @@
   userId String
 }
 model StoreCategory {
-  id         Int      @id @default(autoincrement())
-  order      Int
-  category   Category @relation(fields: [categoryId], references: [id])
-  categoryId Int
-  store      Store    @relation(fields: [storeId], references: [id])
-  storeId    Int
+  id           Int    @id @default(autoincrement())
+  order        Int
+  categoryName String
+  categoryId   Int
+  store        Store  @relation(fields: [storeId], references: [id])
+  storeId      Int
 }
 model User {
   id     String     @id @default(uuid())
```


